}
    id 'com.android.library'
插件 {

android {
    compileSdk 34

    defaultConfig {
        minSdk 26
        targetSdk 34
        versionCode 1
        versionName "1.0"
    }

    buildTypes {
        release {
            minifyEnabled false
        }
    }
}

// ← 这里新增
repositories {
    flatDir {
        dirs 'libs'
    }
}

dependencies {
    implementation(name:'api-101.0.1', ext:'aar')  // ← 指向你的 .aar 文件
}
